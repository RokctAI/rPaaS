# Synthetic Trading — Business Profile

> SYNTHETIC STAND-IN (committed fixture) — this is NOT engine output. The
> O-04 smoke (`verify_o4_smoke.py`) uses this file only when
> `O4_BUSINESS_PROFILE` / `O4_COMPLIANCE_LOG` are unset; the REAL
> StartupOS-generated `business_profile.md` is passed via those env vars
> and is deliberately never committed. This stand-in mirrors only the
> engine's honest shape: unverified fields stay marked Pending.

## 1. Corporate Registry

| Parameter | Detail |
| :--- | :--- |
| **Trading Name** | Synthetic Trading |
| **CIPC Registration Number** | Pending — registration number not verified — add Business Registration Certificate.pdf |
| **SARS Tax Reference** | Pending — tax reference not verified — add Tax_Pin.pdf |
| **Industry** | Import and export logistics |
