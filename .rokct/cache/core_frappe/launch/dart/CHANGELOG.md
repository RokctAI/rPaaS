# Changelog

## 1.1.1

* Restructure the `application/` layer into feature subfolders: `launch_notifier.dart`, `launch_provider.dart`, and `launch_state.dart` moved from `lib/src/common/application/` into `lib/src/common/application/launch/`. Barrel exports in `launch_sdk.dart` updated. No public API changes.
